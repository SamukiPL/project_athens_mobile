/// Why I want to use model instead of normal map? Because those maps in dart are
/// useless and I don't want to have anything with them.
/// :(((
class WordModel {
  final double occurrence;
  final String word;

  WordModel(this.occurrence, this.word);
}