import 'package:flutter/cupertino.dart';

abstract class TileBase extends StatelessWidget {
  TileBase({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}